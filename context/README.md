# context

React Contexts para estado global compartido entre pantallas.

- Cada archivo define un Context, su Provider y (opcionalmente) su hook de consumo.
- Usar solo para estado verdaderamente global: sesión, tema, idioma.
- Ejemplos: `AuthContext.jsx`, `ThemeContext.jsx`.
