# components

Componentes de UI reutilizables sin lógica de negocio: botones, tarjetas, inputs, modales, etc.

- No deben hacer llamadas a APIs ni manejar estado global.
- Reciben todo lo que necesitan por props.
- Ejemplos: `Button.jsx`, `PokemonCard.jsx`, `Avatar.jsx`.
