import { StyleSheet } from 'react-native';
import { COLORS } from '@/config/styles';

export default StyleSheet.create({
    value: {
        fontSize: 20,
        color: COLORS.text,
        textAlign: 'left',
        flexShrink: 1,
    },
});
