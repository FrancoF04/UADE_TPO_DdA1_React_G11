# navigation

Configuración de los navegadores de React Navigation: Stack, Tab y Drawer.

- Cada archivo define un navigator o agrupa navigators relacionados.
- El archivo raíz (p. ej. `AppNavigator.jsx`) conecta todos los navigators.
- Ejemplos: `AppNavigator.jsx`, `AuthStack.jsx`, `BottomTabs.jsx`.
