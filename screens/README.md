# screens

Una pantalla por archivo, cada una corresponde a una ruta/nodo del navegador.

- Usan componentes de `components/` y lógica de `hooks/` o `context/`.
- No contienen lógica de negocio directamente; delegan en hooks y servicios.
- Ejemplos: `HomeScreen.jsx`, `LoginScreen.jsx`, `DetailScreen.jsx`.
